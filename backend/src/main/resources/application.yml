spring:
  application:
    name: ledgerflow-backend
  autoconfigure:
    exclude: org.springframework.boot.security.autoconfigure.UserDetailsServiceAutoConfiguration
  flyway:
    enabled: true
    locations: classpath:db/migration
    community-db-support-enabled: true
    validate-migration-naming: true
  sql:
    init:
      mode: never
  servlet:
    multipart:
      max-file-size: ${LEDGERFLOW_MULTIPART_MAX_SIZE:256MB}
      max-request-size: ${LEDGERFLOW_MULTIPART_REQUEST_MAX_SIZE:257MB}

ledgerflow:
  data-dir: ${LEDGERFLOW_DATA_DIR:${user.home}/.ledgerflow/data}
  database:
    path: ${LEDGERFLOW_DATABASE_PATH:${user.home}/.ledgerflow/data/ledgerflow.db}
  security:
    session-cookie-name: LF_SESSION
    session-ttl: 7d
    reset-token-ttl: 30m
    secure-cookies: ${LEDGERFLOW_SECURE_COOKIES:false}
    same-site: Lax
    allowed-origins: ${LEDGERFLOW_ALLOWED_ORIGINS:http://localhost:5173}
    auth-rate-limit: 30
    auth-rate-window: 1m
  attachments:
    max-size: ${LEDGERFLOW_ATTACHMENT_MAX_SIZE:10MB}
  backups:
    max-encrypted-size: ${LEDGERFLOW_BACKUP_MAX_ENCRYPTED_SIZE:256MB}
    max-uncompressed-size: ${LEDGERFLOW_BACKUP_MAX_UNCOMPRESSED_SIZE:512MB}
    max-entries: ${LEDGERFLOW_BACKUP_MAX_ENTRIES:10000}
    max-attachments: ${LEDGERFLOW_BACKUP_MAX_ATTACHMENTS:5000}
    pbkdf2-iterations: ${LEDGERFLOW_BACKUP_PBKDF2_ITERATIONS:310000}
